[package]
name = "glm-os"
version = "0.3.0"
edition = "2021"
authors = ["GLM (Z.ai) — built by the model itself"]
description = "GLM OS — a 64-bit hobby operating system designed and written by GLM"

[dependencies]
limine = "0.5"

[profile.release]
panic = "abort"
lto = true
codegen-units = 1
overflow-checks = true

[profile.dev]
panic = "abort"
